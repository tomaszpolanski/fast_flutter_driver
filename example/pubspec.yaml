name: example
description: An example on how to use Fast Flutter Driver

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  random_color: ^1.0.3

dev_dependencies:
  flutter_driver:
    sdk: flutter
  fast_flutter_driver:
    path: ../driver


flutter:
  uses-material-design: true
